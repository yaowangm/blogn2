除非我明确要求，否则不要提交代码
永远不要推送代码
运行测试时，先调用source ~/blogn2-env/bin/activate;进入虚拟环境
修改代码后必须验证结果是否符合预期：跑相关单元测试（例如搜索相关改 tests/unit/test_bert_vectorization_services.py 中 TestHierarchicalSearchService）；若改动影响 API/页面，用 curl 或浏览器实测一次
使用scripts/blogn2-service start/stop/restart来启动/关闭/重启本地应用
构建 Docker 时不要使用 --no-cache
使用~/tool/build_blogn_docker_img.sh重新构建和部署docker镜像
使用docker logs -f blogn2-app查看docker日志
整理代码的原则：去掉不必要的代码；合并并复用重复的代码；如有必要，寻找更简洁的方式并重构，
但不要改动任何实际效果；添加必要的注释
---
description: 操作规则
globs:
alwaysApply: true
---
