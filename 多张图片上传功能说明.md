# 多张图片上传功能说明

## 功能概述

本系统支持在编辑文章时上传和管理多张图片，包括主图和多张附件图片。所有图片操作都支持现有图片和新上传图片的不同处理逻辑。

## 功能组件

### 1. 编辑文章表单 (`edit-post-form.js`)
- 主图上传和预览
- 多张图片上传和预览
- 图片删除功能
- 临时文件管理

### 2. 文章内容显示 (`article-content-card.js`)
- 主图显示
- 更多图片网格布局显示
- 图片点击放大查看

## 图片数据结构

### 主图数据
```javascript
// 新上传主图
this.uploadedImage = {
    id: Date.now() + Math.random(),
    url: "/upload/202509/filename.jpg",
    name: "新上传图片",
    relative_path: "202509/filename.jpg",
    filename: "filename.jpg",
    is_temp: true,
    size: 1024000
}

// 现有主图
this.existingImage = {
    url: "/upload/202509/existing.jpg",
    name: "现有主图片",
    relative_path: "202509/existing.jpg"
}
```

### 多张图片数据
```javascript
// 新上传图片
this.uploadedImages = [
    {
        id: Date.now() + Math.random(),
        url: "/upload/202509/filename1.jpg",
        name: "新上传图片",
        relative_path: "202509/filename1.jpg",
        filename: "filename1.jpg",
        is_temp: true,
        size: 1024000
    }
]

// 现有图片
this.existingImages = [
    {
        id: 123,
        url: "/upload/202509/existing1.jpg",
        name: "现有图片",
        relative_path: "202509/existing1.jpg"
    }
]

// 标记删除的图片
this.deletedImages = [
    {
        id: 456,
        url: "/upload/202509/deleted.jpg",
        name: "已删除图片",
        relative_path: "202509/deleted.jpg"
    }
]
```

## 操作说明

### 1. 图片上传操作

#### 1.1 主图上传
**操作：** 选择文件后自动上传到临时目录

**结果：**
- 图片保存到服务器临时目录
- 设置 `this.uploadedImage` 数据
- 更新 `this.formData.attachment` 路径
- 显示主图预览

**限制：**
- 文件类型：jpg, png, gif
- 文件大小：≤1MB
- 最多1张主图

#### 1.2 多张图片上传
**操作：** 选择文件后自动上传到临时目录

**结果：**
- 图片保存到服务器临时目录
- 添加到 `this.uploadedImages` 数组
- 更新 `this.formData.attachments` 数组
- 显示在"其他图片"区域

**限制：**
- 文件类型：jpg, png, gif
- 文件大小：≤1MB
- 总数量：≤10张（包括主图）

### 2. 图片删除操作

#### 2.1 主图删除

**删除新上传主图：**
- 立即删除服务器临时文件
- 清空 `this.uploadedImage`
- 清空 `this.formData.attachment`

**删除现有主图：**
- 不删除服务器文件
- 将图片添加到 `this.deletedImages` 数组
- 清空 `this.existingImage`
- 清空 `this.formData.attachment`

#### 2.2 多张图片删除

**删除新上传图片：**
- 立即删除服务器临时文件
- 从 `this.uploadedImages` 数组中移除
- 更新 `this.formData.attachments` 数组

**删除现有图片：**
- 不删除服务器文件
- 从 `this.existingImages` 数组中移除
- 添加到 `this.deletedImages` 数组
- 更新 `this.formData.attachments` 数组

### 3. 表单提交操作

#### 3.1 保存修改
**提交数据：**
```javascript
{
    attachment: "202509/main.jpg",           // 主图路径
    attachments: [                          // 保留的图片
        { id: 123, relative_path: "202509/img1.jpg" },
        { id: 124, relative_path: "202509/img2.jpg" }
    ],
    deleted_attachments: [                  // 标记删除的图片
        { id: 125, relative_path: "202509/deleted.jpg" }
    ]
}
```

**后端处理：**
- 保存主图到正式目录
- 保存新上传图片到正式目录
- 删除标记为删除的现有图片文件
- 更新数据库记录

#### 3.2 取消修改
**操作：** 点击取消按钮

**结果：**
- 删除所有临时文件（主图和多张图片）
- 清空所有上传相关数据
- 恢复原始状态
- 返回上一页

## 显示效果

### 1. 编辑页面显示

#### 主图区域
- 显示当前主图（新上传或现有）
- 显示图片名称和大小
- 提供删除按钮

#### 多张图片区域
- 标题显示"其他图片 (数量)张"
- 网格布局显示所有图片
- 每张图片显示名称和大小
- 提供删除按钮

### 2. 文章页面显示

#### 主图显示
- 文章顶部显示主图
- 支持点击放大查看

#### 更多图片显示
- 标题显示"更多图片 (数量)"
- 根据图片数量智能布局：
  - 1张：单列靠左
  - 2张：双列靠左
  - 3张：三列居中
  - 4张：双列居中
  - 5-6张：三列居中
  - 7-8张：四列居中
  - 9张：三列居中
  - 10张：五列居中
  - 超过10张：自适应布局

#### 响应式设计
- **桌面端**：根据数量设置列数
- **平板端**（≤768px）：最多2列
- **手机端**（≤480px）：单列布局

## 技术实现

### 1. 文件上传
- 使用 `FormData` 上传文件
- 添加 `temp=true` 参数标记临时文件
- 上传到 `/api/upload?temp=true` 接口

### 2. 文件删除
- 临时文件：调用 `/api/temp-upload/{filename}` DELETE接口
- 现有文件：标记删除，提交时处理

### 3. 事件处理
- 使用事件委托处理动态添加的删除按钮
- 通过 `data-image-id` 属性识别图片
- 支持主图（`data-image-id="main"`）和多张图片删除

### 4. 状态管理
- 使用多个数组管理不同状态的图片
- 实时同步 `formData` 数据
- 支持撤销和重做操作

## 错误处理

### 1. 上传错误
- 文件类型不支持：显示错误提示
- 文件过大：显示错误提示
- 网络错误：显示重试提示

### 2. 删除错误
- 临时文件删除失败：记录错误日志，不影响操作
- 网络错误：显示错误提示

### 3. 数据同步
- 实时更新预览显示
- 确保数据一致性
- 支持操作撤销

## 安全考虑

### 1. 文件验证
- 客户端验证文件类型和大小
- 服务端再次验证文件安全性

### 2. 权限控制
- 只有文章作者可以编辑图片
- 管理员可以管理所有文章图片

### 3. 临时文件清理
- 取消时自动清理临时文件
- 防止服务器存储空间浪费

## 性能优化

### 1. 图片预览
- 使用 `loading="lazy"` 延迟加载
- 固定图片尺寸避免布局重排

### 2. 文件上传
- 支持多文件同时上传
- 异步处理不阻塞界面

### 3. 内存管理
- 及时清理不需要的数据
- 避免内存泄漏

## 使用建议

### 1. 图片选择
- 建议使用jpg格式，文件大小控制在500KB以内
- 图片尺寸建议800x600像素以上

### 2. 操作流程
- 先上传主图，再上传其他图片
- 删除图片前确认操作意图
- 定期保存避免数据丢失

### 3. 性能考虑
- 单次上传图片数量不要超过5张
- 大文件建议先压缩再上传
- 网络不稳定时建议分批上传

---

*最后更新：2024年12月*
