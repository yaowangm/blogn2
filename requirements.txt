# Core dependencies
# 重要：以下版本约束为避免已知兼容性问题，升级前请查文档与测试
# - bcrypt: passlib 依赖 __about__，bcrypt 4.1+ 已移除，导致登录 401
# - SQLAlchemy: sqlmodel 0.0.14 要求 <2.1
# - pydantic: 与 FastAPI/sqlmodel/pydantic-settings 保持一致，避免 2.11+ 未验证变更
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets>=12.0
sqlmodel==0.0.14
SQLAlchemy>=2.0,<2.1
asyncpg==0.29.0
python-dotenv==1.0.0
psycopg2-binary==2.9.9
pydantic-settings==2.0.3
pydantic>=2.0,<2.11
portalocker==2.8.2
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0,<4.1
python-multipart==0.0.6

# Cache dependencies
fastapi-cache2==0.2.1
redis==5.0.1

# BERT Vectorization dependencies
# sentence-transformers 与 transformers 4.35 兼容，避免拉取需 4.41+ 的新版
transformers==4.35.0
torch==2.2.0
numpy<2.0,>=1.24.0
scikit-learn>=1.3.0
pgvector==0.2.4
sentence-transformers>=2.2.2,<3.0

# Testing dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-timeout==2.4.0
httpx==0.25.2
aiosqlite==0.19.0